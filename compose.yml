services:
  materias-primas:
    build: .
    container_name: materias-primas

    env_file:
      - .env

    ports:
      - "8000:8000"

    volumes:
      - ./db.sqlite3:/app/db.sqlite3

    restart: unless-stopped